---
import { getCollection } from 'astro:content';
import BaseHead from '../../components/BaseHead.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import FormattedDate from '../../components/FormattedDate.astro';

const posts = (await getCollection('work', ({ data }) => !data.draft)).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);
---

<html lang="en">
	<head>
		<BaseHead
			title="Work · Kenrick Tjandra"
			description="Robotics engineering case studies and project notes from Raise Robotics."
		/>
	</head>
	<body>
		<Header />
		<main>
			<section class="hero">
				<h1>Work</h1>
				<p class="lede">
					Case studies and project notes from building manipulation stacks. The boring failures
					included.
				</p>
			</section>

			{posts.length > 0 ? (
				<ul class="post-list">
					{posts.map((p) => (
						<li>
							<span class="post-date">
								<FormattedDate date={p.data.pubDate} />
							</span>
							<div>
								<div class="post-title">
									<a href={`/work/${p.id}`}>{p.data.title}</a>
								</div>
								<p class="post-desc">{p.data.description}</p>
							</div>
						</li>
					))}
				</ul>
			) : (
				<p class="empty-state">Nothing here yet. Case studies coming as I get permission to publish them.</p>
			)}
		</main>
		<Footer />
	</body>
</html>
